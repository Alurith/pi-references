{
  "name": "pi-references",
  "version": "0.1.0",
  "description": "Pi extension that implements OpenCode-style references",
  "license": "MIT",
  "files": ["index.ts", "src", "README.md", "LICENSE"],
  "type": "module",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Alurith/pi-references.git"
  },
  "keywords": [
    "pi-package",
    "pi-extension",
    "references"
  ],
  "scripts": {
    "check": "tsc --noEmit",
    "test": "node --import tsx --test test/*.test.ts"
  },
  "pi": {
    "extensions": [
      "./index.ts"
    ]
  },
  "dependencies": {
    "jsonc-parser": "^3.3.1"
  },
  "devDependencies": {
    "@earendil-works/pi-coding-agent": "^0.84.3",
    "@earendil-works/pi-tui": "^0.84.3",
    "@types/node": "^22.20.1",
    "tsx": "^4.23.1",
    "typescript": "^5.9.3"
  },
  "peerDependencies": {
    "@earendil-works/pi-coding-agent": "*"
  },
  "peerDependenciesMeta": {
    "@earendil-works/pi-coding-agent": {
      "optional": true
    }
  }
}
